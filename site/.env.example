# The public site is read-only and needs no BODS credential.
BBB_LIVE_DB=/var/lib/bristolbusbot/collector/live.db
BBB_TIMETABLE_DB=/var/lib/bristolbusbot/pipeline/timetable.db
BBB_ENFORCE_HTTPS=true
# Required in production; install it with deploy/configure_carto_key.py.
BBB_CARTO_BASEMAP_KEY=
