# Non-secret expiry metadata for the fine-grained token stored separately at
# /etc/bristolbusbot/timetable-delivery.token. The configuration helper writes
# both root-owned files with mode 0600.
BBB_GITHUB_TOKEN_EXPIRES_UTC=2027-01-01T00:00:00Z
